-- ════════════════════════════════════════════════════════════════════════════
-- 회원 접속 표시 — last_seen_at 컬럼
-- ────────────────────────────────────────────────────────────────────────────
-- 사용법: Supabase SQL Editor → 새 쿼리 → 붙여넣기 → Run
-- ════════════════════════════════════════════════════════════════════════════

ALTER TABLE users ADD COLUMN IF NOT EXISTS last_seen_at timestamptz;
CREATE INDEX IF NOT EXISTS idx_users_last_seen_at ON users(last_seen_at DESC NULLS LAST);

-- ════════════════════════════════════════════════════════════════════════════
-- 적용 후 동작:
--   1) 접속한 회원의 클라이언트가 60초마다 /api/users/heartbeat 호출
--   2) last_seen_at 갱신
--   3) 회원명단 페이지 — 3분 이내 활동한 회원에게 초록 점 + "접속중" 뱃지
-- ════════════════════════════════════════════════════════════════════════════
